; The @tanstack/react-start peer wants vite >=7 while vitest pulls vite ^5.
; These are optional peers that don't affect the published build (tsup uses
; esbuild, not vite), so use legacy peer resolution for this repo's own
; dev/CI install. This file is repo-local and is not published (see "files").
legacy-peer-deps=true
